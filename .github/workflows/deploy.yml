name: Deploy Nginx on AWS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Install Terraform
        uses: hashicorp/setup-terraform@v2

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v3
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_KEY }}
          aws-region: ap-south-1

      - name: Terraform Init & Apply
        run: |
          cd terraform
          terraform init
          terraform apply -auto-approve

      - name: Install Ansible
        run: sudo apt update && sudo apt install -y ansible

      - name: Configure SSH Key
        run: |
          mkdir -p /home/mihir/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" | tr -d '\r' > /home/mihir/.ssh/mihir.pem
          chmod 600 /home/mihir/.ssh/mihir.pem
          eval "$(ssh-agent -s)"
          ssh-add /home/mihir/.ssh/mihir.pem


      - name: Add SSH Host Key to Known Hosts
        run: |
          ssh-keyscan -H $(grep -oP '(?<=ansible_host=)[^ ]+' ansible-setup/inventory) >> /home/mihir/.ssh/known_hosts
      
      - name: Run Ansible Playbook
        run: |
          cd ansible-setup
          ansible-playbook -i ansible-setup/inventory ansible-setup/nginx-setup.yml
